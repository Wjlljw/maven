spring:
  application:
    name: stock-service

  cloud:
    nacos:
      config:
        enabled: true
        server-addr: localhost:8848
        group: DEFAULT_GROUP
        file-extension: yaml

      discovery:
        enabled: true
        server-addr: localhost:8848
        group: DEFAULT_GROUP