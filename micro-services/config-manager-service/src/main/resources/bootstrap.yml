spring:
  application:
    name: config-manager-service

  cloud:
    nacos:
      config:
        enabled: true
        server-addr: localhost:8848
        group: DEFAULT_GROUP
        file-extension: yaml

      discovery:
        enabled: false
        server-addr: localhost:8848
        group: DEFAULT_GROUP
