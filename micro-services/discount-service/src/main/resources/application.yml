server:
  port: 8082
  
# Actuator配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,env
  endpoint:
    health:
      show-details: always
    env:
      show-values: always

# 注意：
# 1. 数据库配置已迁移到 Nacos 配置中心 (discount-service.yml)
# 2. 应用名称和服务发现配置在 bootstrap.yml 中
# 3. JPA 配置已迁移到 Nacos 配置中心
# 4. 所有业务相关的配置都可以通过 Nacos 动态更新